[pytest]
testpaths = tests
addopts = -m "not tck"
markers =
    unit: fast tests that do not require the native DataWeave library
    integration: tests that execute the staged native DataWeave library
    tck: DataWeave conformance tests excluded from normal test runs
